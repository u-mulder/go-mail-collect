package http

// Интерфейс определяет действия над сущностью EmailAccount
type _ interface {
	//	//CreateAccount(EmailAccountDto dto) (EmailAccount, error)
	//	//GetById(EmailAccountId id) (EmailAccount, error)
	//	//UpdateAccount(EmailAccountId id, EmailAccountDto dto) (EmailAccount, error)
	//	//DeleteAccount(EmailAccountId id) (EmailAccount, error)
}

// Где реализуются методы? Тут же? Отдельно?
